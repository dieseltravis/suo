{
  "version": "{{environment.version}}",
  "name": "{{title}}",
  "launch_path": "/index.html",
  "description": "{{description}}",
  "icons": {
    "8": "{{image}}",
    "16": "{{image}}",
    "32": "{{image}}",
    "48": "{{image}}",
    "64": "{{image}}",
    "128": "{{image}}"
  },
  "developer": {
    "name": "{{author}}",
    "url": "{{host}}{{app}}{{home}}"
  },
  "installs_allowed_from": [
    "*"
  ],
  "default_locale": "{{lang}}",
  "permissions": {
  },
  "locales": {
    "{{lang}}": {
      "name": "{{title}}",
      "description": "{{description}}"
    }
  }
}